# Используем официальный образ Node.js в качестве базы
FROM node:alpine

# Устанавливаем рабочий каталог в /app
WORKDIR /app

# Копируем файлы package*.json в рабочий каталог
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем код приложения в рабочий каталог
COPY . .

# Открываем порты
EXPOSE 80

EXPOSE 443

# Запускаем команду для запуска сервера разработки
CMD ["npm", "start"]